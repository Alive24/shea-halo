interface:
  display_name: "Shea Halo Agent Review"
  short_description: "Trigger one workflow-backed external review"
  default_prompt: "Use $shea-agent-review to trigger the configured external Review backend for one named Shea Halo issue and read back its result."
